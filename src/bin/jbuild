;; -*- mode: lisp; -*-
(jbuild_version 1)

(executable
 ((name rpiterm)
  (public_name rpiterm)
  (modules (rpiterm))
  (libraries (operations
              prometheus
              prometheus-app.unix))
  ;; (flags (-ccopt -static))
 ))
