;; -*- mode: lisp; -*-
(jbuild_version 1)

(executable
 ((name rpiterm)
  (public_name rpiterm)
  (preprocess (pps (lwt_ppx)))
  (libraries (rpiterm_lib
              prometheus
              prometheus-app.unix))
  (flags (-safe-string))
  (ocamlopt_flags (-O2 -safe-string))))
