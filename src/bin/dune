;; -*- tuareg-dune -*-

(executable
 (name rpiterm)
 (public_name rpiterm)
 (preprocess (pps lwt_ppx))
 (libraries cmdliner
            rpiterm_lib
            prometheus-app.unix)
 (flags (-safe-string))
 (ocamlopt_flags (:standard -O2 -safe-string)))

(env
 (static_build
  (ocamlopt_flags (:standard -O2 -safe-string -ccopt -static))))
