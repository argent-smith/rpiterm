;; -*- mode: lisp; -*-
(jbuild_version 1)

(library
 ((name rpiterm_lib)
  (wrapped false)
  (libraries (prometheus
              prometheus-app.unix))
  (preprocess (pps (lwt.ppx)))
  (flags (-safe-string))
  (ocamlopt_flags (-O2 -safe-string))
  ))
